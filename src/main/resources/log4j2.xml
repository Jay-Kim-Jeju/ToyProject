<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p [%c{1.}] %m%n"/>
        </Console>
    </Appenders>

    <Loggers>

        <!-- MyBatis / log4jdbc (if you use it) -->
        <Logger name="jdbc" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="jdbc.sqltiming" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="jdbc.resultset" level="OFF"/>
        <Logger name="jdbc.resultsettable" level="INFO"/>
        <Logger name="jdbc.audit" level="OFF"/>

        <!-- Your app -->
        <Logger name="toy.admin" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Spring -->
        <Logger name="org.springframework" level="WARN"/>

        <!-- Transaction / AOP verification -->
        <Logger name="org.springframework.transaction" level="DEBUG"/>
        <Logger name="org.springframework.jdbc.datasource" level="DEBUG"/>
        <Logger name="org.springframework.aop" level="DEBUG"/>

        <!-- IMPORTANT: Root must exist so logs have somewhere to go -->
        <Root level="DEBUG">
            <AppenderRef ref="Console"/>
        </Root>

    </Loggers>
</Configuration>
