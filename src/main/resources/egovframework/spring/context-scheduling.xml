<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/task    http://www.springframework.org/schema/task/spring-task.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        Purpose:
        - Use Spring @Scheduled instead of Quartz for simpler maintenance.
        - You can control thread pool for scheduled jobs here.
    -->

    <task:scheduler id="taskScheduler" pool-size="4" />
    <task:annotation-driven scheduler="taskScheduler" />

    <!-- Ensure your scheduler classes are discovered -->
    <context:component-scan base-package="toy" />

</beans>
