<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- @Controller, @RequestMapping 이 있는 클래스 스캔 (관리자 전용 경로만 포함) -->
    <context:component-scan base-package="toy">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <!--<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>-->
    </context:component-scan>

    <!-- Spring MVC 설정 활성화 (기본 설정 포함: @RequestMapping 등) -->
    <mvc:annotation-driven />

    <!-- JSP View Resolver 설정 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- From external Spring docs style -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.support.StandardServletMultipartResolver" />


    <!-- 예외 처리 기본 뷰 설정 -->
    <bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
        <property name="defaultErrorView" value="com/error" />
        <property name="exceptionAttribute" value="exception" />
    </bean>


    <!-- 다국어 메시지 처리 -->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basenames">
            <list>
                <value>classpath:messages.message</value>
            </list>
        </property>
        <property name="defaultEncoding" value="UTF-8" />
    </bean>



    <!-- 관리자 인터셉터: 인증 등 필요한 요청만 필터링
    / 로그인구현후 주석제거-->
    <!--<mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            &lt;!&ndash; 로그인 관련 요청은 제외 &ndash;&gt;
            <mvc:exclude-mapping path="/admin/login.ac"/>
            <mvc:exclude-mapping path="/admin/logout.ac"/>
            <mvc:exclude-mapping path="/admin/actionLogin.doax"/>
            <bean class="toy.com.interceptor.AdminAuthInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>-->

</beans>
